{
  "name": "@mcp-platform/platform-smoke",
  "version": "0.1.0",
  "description": "Local stdio smoke server for MCP Platform core.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@mcp-platform/core": "file:../../packages/core"
  }
}
